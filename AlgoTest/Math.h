#pragma once
#include <vector>

using namespace std;

int BalanceNumbers(int L, int R);


int numIslands(vector<vector<char>>& grid);


int BinarySearch(vector<int>& nums, int l, int r, int num);
vector<vector<int>> threeSum(vector<int>& nums);
